import React from 'react';
import Header from "../../components/header/Header";

const Favourites = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Favourites;